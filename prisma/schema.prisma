generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL_MONGO")
}

model user {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  age       Int
  createdAt DateTime
  height    Float
  role      String
}
