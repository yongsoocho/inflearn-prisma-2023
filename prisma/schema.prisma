generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL_MONGO")
}

model User {
  userId    String   @id @map("_id") @db.ObjectId @default(auto())
  age       Int      @db.Int
  height    Float    @db.Double
  createdAt DateTime @default(now()) @db.Timestamp
  role      Role

  @@map("user")
}

enum Role {
  ADMIN
  USER
  MANAGER
}
